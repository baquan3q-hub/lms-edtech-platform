import { getAttendanceOverview } from './lib/actions/attendance'; import { getClassAttendanceSessions } from './lib/actions/attendance-points'; async function run() { const month = 4; const year = 2026; const {data: ovData} = await getAttendanceOverview(month, year); if(!ovData || ovData.classSummaries.length===0) return console.log('No overview'); const cId = ovData.classSummaries[0].classId; console.log('Found Class:', cId); const sess = await getClassAttendanceSessions(cId, month, year); console.log('Sessions Data Length:', sess.data.length); console.log('Sess Err:', sess.error); } run();